<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Sports Events</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(to right, #1e1e2f, #2a2a3e);
      color: white;
      display: flex;
    }

    /* Sidebar */
    .sidebar {
      width: 250px;
      background: rgba(0, 0, 0, 0.3);
      backdrop-filter: blur(10px);
      padding: 30px 20px;
      height: 100vh;
      position: fixed;
      top: 0;
      left: 0;
      border-right: 1px solid rgba(255, 255, 255, 0.1);
    }

    .logo {
      font-size: 28px;
      font-weight: bold;
      margin-bottom: 40px;
    }

    .logo span {
      color: #ff6b6b;
    }

    .navwords {
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .navwords a {
      color: white;
      text-decoration: none;
      font-weight: 500;
      font-size: 18px;
      transition: color 0.3s ease;
      position: relative;
    }

    .navwords a:hover {
      color: #ff6b6b;
    }

    .navwords a::after {
      content: '';
      position: absolute;
      width: 0;
      height: 2px;
      bottom: -4px;
      left: 0;
      background-color: #ff6b6b;
      transition: width 0.3s ease;
    }

    .navwords a:hover::after {
      width: 100%;
    }

    /* Main Content */
    .main-content {
      margin-left: 250px;
      padding: 40px 30px;
      width: calc(100% - 250px);
    }

    .event-container {
      display: flex;
      flex-direction: column;
      gap: 30px;
      max-width: 1000px;
      margin: auto;
    }

    .event-card {
      background: rgba(255, 255, 255, 0.08);
      backdrop-filter: blur(12px);
      border-radius: 16px;
      padding: 25px;
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.4);
      display: flex;
      flex-direction: column;
      align-items: center;
      transition: transform 0.3s;
    }

    .event-card:hover {
      transform: translateY(-5px);
    }

    .teams {
      display: flex;
      align-items: center;
      justify-content: space-around;
      width: 100%;
      gap: 20px;
      margin-bottom: 20px;
    }

    .team {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .team img {
      width: 90px;
      height: 90px;
      border-radius: 50%;
      object-fit: cover;
      margin-bottom: 10px;
      border: 3px solid #ff6b6b;
    }

    .team-name {
      font-weight: bold;
      font-size: 1.2em;
      text-align: center;
    }

    .details {
      text-align: center;
    }

    .details p {
      margin: 6px 0;
      font-size: 16px;
    }

    /* Responsive */
    {
      .sidebar {
        display: none;
      }

      .main-content {
        margin-left: 0;
        width: 100%;
        padding: 20px;
      }

      .teams {
        flex-direction: column;
      }
    }
  </style>
</head>
<body>


  <!-- Main Content -->
  <div class="main-content">
    <div class="event-container">

      <!-- Example Event Card -->
      <div class="event-card">
        <div class="teams">
          <div class="team">
                        <img src="/images/th.png" alt="Team 1 Logo">
                        

            <div class="team-name">Zamalek SC</div>
          </div>
          <div class="team">
                        <img src="/images/th2.png" alt="Team 2 Logo">
            <div class="team-name">Alahly SC</div>
          </div>
        </div>
        <div class="details">
          <p><strong>Sport:</strong> Football</p>
          <p><strong>Tournament:</strong> Champions League</p>
          <p><strong>Stage:</strong> Semi Final</p>
        </div>
      </div>

      <!-- More events can be dynamically added here -->

    </div>
  </div>

</body>
</html>

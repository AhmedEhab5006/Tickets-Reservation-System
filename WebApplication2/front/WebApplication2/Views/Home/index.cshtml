<!DOCTYPE html>
<html lang="en">
<head>

    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tickets Reservation</title>
    <style>
        /* Add this to your home.css or in the head */
        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: url('https://source.unsplash.com/random/1920x1080/?concert,sports') no-repeat center center fixed;
            background-size: cover;
            min-height: 100vh;
            color: white;
        }

        /* Glass overlay for the entire hero section */
        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(5px);
            -webkit-backdrop-filter: blur(5px);
            z-index: -1;
        }

        .hero {
            position: relative;
            height: 100vh;
            width: 100%;
            background-image: url('/images/background3.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        /* Navigation bar with glass effect */
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 50px;
            background: rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .logo {
            font-size: 28px;
            font-weight: bold;
        }

            .logo span {
                color: #ff6b6b;
            }

        /* Navigation links with hover effect */
        .navwords {
            display: flex;
            gap: 30px;
            list-style: none;
        }

            .navwords a {
                color: white;
                text-decoration: none;
                font-weight: 500;
                transition: color 0.3s ease;
                position: relative;
            }

                .navwords a:hover {
                    color: #ff6b6b;
                }

                /* Optional: Add underline effect on hover */
                .navwords a::after {
                    content: '';
                    position: absolute;
                    width: 0;
                    height: 2px;
                    bottom: -5px;
                    left: 0;
                    background-color: #ff6b6b;
                    transition: width 0.3s ease;
                }

                .navwords a:hover::after {
                    width: 100%;
                }

        /* Main content with glass effect */
        .content {
            max-width: 800px;
            margin: 150px auto;
            padding: 40px;
            text-align: center;
            background: rgba(0, 0, 0, 0.4);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
        }

        .hook {
            font-size: 3rem;
            margin-bottom: 20px;
        }

            .hook span {
                color: #ff6b6b;
            }

        .hook2 {
            font-size: 1.2rem;
            margin-bottom: 30px;
            opacity: 0.9;
        }

        /* Glass button */
        .btn {
            display: inline-block;
            padding: 12px 30px;
            background: #ff6b6b;
            backdrop-filter: blur(5px);
            color: white;
            text-decoration: none;
            border-radius: 50px;
            border: 1px solid rgba(255, 255, 255, 0.2);
            font-weight: bold;
            transition: all 0.3s ease;
        }

            .btn:hover {
                color: #ff6b6b;
                background: white;
                transform: translateY(-3px);
                box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
            }
    </style>
</head>
<body>

    <div class="content">
        <h1 class="hook">Book Your Event <span>Now</span></h1>
        <p class="hook2">Find and reserve your favorite sports or entertainment events in seconds!</p>
        <a href="@Url.Action("ReserveTicket", "Home")" class="btn">Reserve a Ticket</a>
    </div>
    </div>
</body>
</html>